<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="arfesan.remzi">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/arfesan_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/arfesan_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GirisekraniActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:label="Ana Ekran"/>
        <activity android:name=".RaporYaz" android:label="Rapor Yaz"/>
        <activity
            android:name=".Grafikler"
            android:label="Grafikler" />
    </application>

</manifest>